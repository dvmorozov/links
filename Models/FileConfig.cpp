#include "FileConfig.h"
#include <stdexcept>

namespace Bookmarks
{
    std::wstring FileConfig::GetValue(std::wstring name)
    {
        throw new std::runtime_error("Method isn't implemented.");
    }
}
